{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    {% for row in value %}
        {{row|slice(5)}}
        {% if not loop.last %}
            ;
        {% endif %}
    {% endfor %}
{% endblock %}